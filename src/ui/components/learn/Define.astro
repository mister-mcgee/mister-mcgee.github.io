---
import D from "@components/learn/Define";
export type Props = {
  word        : string;
  definition ?: string;
}
const { word, definition } = Astro.props;
---

<D {word} {definition} client:only><slot/></D>