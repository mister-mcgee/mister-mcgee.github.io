---
import D from "@components/learn/Define";
export type Props = {
  term  : string;
  html ?: string;
}
const { term, html } = Astro.props;
const hasChildren = Astro.slots.default;
---

<D {term} {html} client:only>
  {hasChildren && <slot />}
</D>