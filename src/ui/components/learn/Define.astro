---
import D from "@components/learn/Define";
export type Props = {
  word        : string;
  definition ?: string;
  children   ?: any
}
const { word, definition, children } = Astro.props;
---

<D {word} {definition} client:only>{ children }</D>