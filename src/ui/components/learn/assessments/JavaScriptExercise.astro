---
import J from "@components/learn/assessments/JavaScriptExercise";

const {   title,
  instructions,
  starterCode,
  tests, 
  groupId,
  taskId,
  doSubmit } = Astro.props; 
---

<J {title} {instructions} {starterCode} {tests} {groupId} {taskId} {doSubmit} client:only/>