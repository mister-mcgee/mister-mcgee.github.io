---
import P from "@components/learn/assessments/PythonExercise";

const {   title,
  instructions,
  starterCode,
  tests, 
  groupId,
  taskId,
  doSubmit} = Astro.props; 
---

<P {title} {instructions} {starterCode} {tests} {groupId} {taskId} {doSubmit} client:only/>